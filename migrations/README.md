# Migrations

这个目录用于存放后续 PostgreSQL schema migration。

当前第一版还没有正式落数据库，因此这里先保留入口。

下一步进入 PostgreSQL 持久化时，建议至少补：

1. `project`
2. `scan_task`
3. `resource_item`
4. `issue_item`
5. `issue_action_log`
6. `attachment_file`